# Simple pokedex written in Go!

## Usage

To run project, download code from master brunch, then, provided you have Go compiler installed, run ``` go build ``` and ``` pokedex.exe ```.
Write ``` help ``` to see possible commands! Have fun! 